services:
  - type: web
    name: fraud-scoring-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn scoring_service:app --host 0.0.0.0 --port $PORT
